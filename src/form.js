const rootSection = document.getElementById('root');
const logIn = `
    <form action="">
        <input type="text" placeholder="Username">
        <input type="password" placeholder="Password">
        <button id="btnLogin">Login</button>
    </form>
`;
rootSection.appendChild(logIn);
